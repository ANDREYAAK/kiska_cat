# 🚀 Быстрый старт с автосохранением в PostgreSQL

## Что сделано

✅ **Автосохранение при изменениях:**
- Сохраняет через 2 секунды после последнего изменения объекта
- Работает при перетаскивании, повороте, добавлении/удалении объектов
- Минимальная нагрузка на БД (batch updates)

✅ **Локальный PostgreSQL сервер:**
- Простой Node.js сервер
- Готовые SQL скрипты для инициализации
- Работает на localhost

✅ **Гибридное сохранение:**
- Локально (IndexedDB) - для быстрого доступа
- На сервере (PostgreSQL) - для надёжности

## Установка (5 минут)

### 1. Установить зависимости сервера

```bash
cd server
npm install
```

### 2. Создать базу данных

```bash
# В терминале PostgreSQL
psql -U postgres
CREATE DATABASE game_db;
\q
```

### 3. Инициализировать таблицы

```bash
psql -U postgres -d game_db -f server/init.sql
```

### 4. Настроить сервер

Создайте `server/.env`:
```env
PORT=3001
DB_HOST=localhost
DB_PORT=5432
DB_NAME=game_db
DB_USER=postgres
DB_PASSWORD=ваш_пароль
```

### 5. Запустить сервер

```bash
cd server
npm start
```

Должно появиться: `🚀 Сервер запущен на http://localhost:3001`

### 6. Настроить игру

Создайте `.env` в корне проекта:
```env
VITE_API_URL=http://localhost:3001
```

### 7. Перезапустить игру

Остановите и запустите заново:
```bash
npm run dev
```

## Проверка работы

1. Откройте игру в браузере
2. Перетащите объект на карте
3. Подождите 2 секунды
4. В консоли должно появиться: `[AutoSaveManager] Save complete`
5. Перезагрузите страницу (F5)
6. Объекты должны остаться на месте! 🎉

## Как это работает

### Автосохранение
- При изменении объекта → ждёт 2 секунды
- Если за это время были ещё изменения → сбрасывает таймер
- Сохраняет все изменения разом (batch)

### Архитектура
```
Игра (браузер)
    ↓ изменение объекта
ObjectRegistry → onChange()
    ↓
AutoSaveManager → debounce 2 сек
    ↓
SaveManager → HybridStorageAdapter
    ├─→ IndexedDB (локально)
    └─→ PostgreSQL (сервер)
```

## Для разработки

Если не хотите запускать сервер:
- Игра будет работать только с локальными сохранениями
- Просто не создавайте `.env` с `VITE_API_URL`
- Всё сохранится в IndexedDB браузера

## Проблемы?

**Сервер не запускается:**
- Проверьте, что PostgreSQL запущен
- Проверьте пароль в `server/.env`
- Проверьте, что база данных `game_db` создана

**Сохранения не работают:**
- Откройте консоль браузера (F12)
- Проверьте ошибки
- Убедитесь, что сервер запущен на порту 3001

**Объекты не сохраняются:**
- Проверьте, что объекты размещены игроком (не системные)
- Проверьте консоль на ошибки автосохранения

## Готово! 🎮

Теперь можно строить город, и всё будет автоматически сохраняться!
